{{- if and .Values.airflow.enabled .Values.airflow.ingress.enabled }}
{{- $fullName := printf "%s-%s" (include "qmig.airflow.fullname" .) "webserver" -}}
{{- $svcPort := .Values.airflow.webserver.service.port -}}
{{- if .Values.airflow.ingress.className }}
  {{- if not (hasKey .Values.airflow.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.airflow.ingress.annotations "kubernetes.io/ingress.class" .Values.airflow.ingress.className}}
  {{- end }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "qmig.airflow.labels" . | nindent 4 }}
  {{- with .Values.airflow.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.airflow.ingress.className }}
  ingressClassName: {{ .Values.airflow.ingress.className }}
  {{- end }}
  {{- if .Values.airflow.ingress.tls }}
  tls:
    {{- range .Values.airflow.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- if .Values.airflow.ingress.host }}
    - host: {{ .Values.airflow.ingress.host | quote }}
      http:
    {{- else }}
    - http:
    {{- end }}
        paths:
          - path: /airflow
            pathType: Prefix
            backend:
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
{{- end }}