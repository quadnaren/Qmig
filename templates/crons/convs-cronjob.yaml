{{- $pvcTemp := printf "%s-%s" (include "qmig.convs.fullname" .) "empty" -}}
{{- $pvc := include "qmig.pv.shared" . -}}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ template "qmig.convs.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "qmig.convs.labels" . | nindent 4 }}
  {{- with .Values.convs.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  concurrencyPolicy: {{ .Values.convs.concurrencyPolicy }}
  failedJobsHistoryLimit: {{ .Values.convs.failedJobsHistoryLimit }}
  schedule: {{ .Values.convs.schedule | quote }}
  successfulJobsHistoryLimit: {{ .Values.convs.successfulJobsHistoryLimit }}
  startingDeadlineSeconds: {{ .Values.convs.startingDeadlineSeconds }}
  suspend: {{ .Values.convs.suspend }}
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: {{ .Values.convs.ttlSecondsAfterFinished }}
      backoffLimit: {{ .Values.convs.backoffLimit }}
      template:
        metadata:
          labels:
            {{- include "qmig.convs.labels" . | nindent 12 }}
          {{- with .Values.convs.annotations }}
          annotations:
            {{ toYaml . | nindent 12 }}
          {{- end }}
        spec:
          {{- with .Values.convs.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.convs.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          serviceAccountName: {{ template "qmig.serviceAccountName" . }}
          {{- with .Values.convs.affinity }}
          affinity:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.convs.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.convs.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          containers:
            - name: {{ .Values.convs.name }}
              image: "{{ .Values.convs.image.repository }}:{{ .Values.convs.image.tag }}"
              imagePullPolicy: {{ .Values.convs.image.pullPolicy }}
              resources:
                {{- toYaml .Values.convs.resources | nindent 16 }}
              {{- with .Values.convs.containerSecurityContext }}
              securityContext:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              env:
                - name: POD_NAMESPACE
                  valueFrom:
                    fieldRef:
                      apiVersion: v1
                      fieldPath: metadata.namespace
                - name: POD_NAME
                  valueFrom:
                    fieldRef:
                      apiVersion: v1
                      fieldPath: metadata.name
                {{- range $name, $value := .Values.convs.env }}
                - name: {{ $name }}
                  value: {{ $value | quote}}
                {{- end }}
                {{- range .Values.convs.envSecret }}
                - name: {{ .name }}
                  valueFrom:
                    secretKeyRef:
                      name: {{ .secretRef }}
                      key: {{ .secretKey }}
                {{- end }}
              volumeMounts:
                - mountPath: /mnt/pypod
                  {{- if and .Values.globals.persistentVolume.subPath (ne (include "qmig.cloud" .) "minikube") }}
                  subPath: {{ .Values.globals.persistentVolume.subPath }}
                  {{- end }}
                  name: {{ $pvc }}
                - mountPath: /app/tmp
                  name: {{ $pvcTemp }}
                - mountPath: /tmp
                  name: {{ $pvcTemp }}
              {{- if .Values.convs.extraVolumeMounts }}
                {{- toYaml .Values.convs.extraVolumeMounts | nindent 16 }}
              {{- end }}
          restartPolicy: {{ .Values.convs.restartPolicy }}
          volumes:
            - name: {{ $pvc }}
              {{- if .Values.globals.persistentVolume.enabled }}
              persistentVolumeClaim:
                claimName: {{ if .Values.globals.persistentVolume.existingClaim }}{{ .Values.globals.persistentVolume.existingClaim }}{{- else }}{{ template "qmig.convs.fullname" . }}{{- end }}
              {{- else }}
              emptyDir: {}
              {{- end }}
            - name: {{ $pvcTemp }}
              emptyDir: {}
          {{- if .Values.convs.extraVolumes }}
            {{- toYaml .Values.convs.extraVolumes | nindent 12 }}
          {{- end }}