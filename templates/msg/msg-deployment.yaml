{{- $pvcConf := printf "%s-%s" (include "qmig.msg.fullname" .) "config" -}}
{{- $pvc := include "qmig.msg.fullname" . -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "qmig.msg.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "qmig.msg.labels" . | nindent 4 }}
  {{- with .Values.msg.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.msg.replicaCount }}
  selector:
    matchLabels:
      {{- include "qmig.msg.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.msg.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "qmig.msg.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.msg.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.msg.securityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ template "qmig.serviceAccountName" . }}
      {{- with .Values.msg.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.msg.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.msg.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Values.msg.name }}
          image: "{{ .Values.msg.image.repository }}:{{ .Values.msg.image.tag }}"
          imagePullPolicy: {{ .Values.msg.image.pullPolicy }}
          ports:
            - name: {{ .Values.msg.name }}-tcp
              containerPort: 6379
              protocol: TCP
          resources:
            {{- toYaml .Values.msg.resources | nindent 12 }}
          args:
          {{- if .Values.msg.auth }}
            - "/etc/keydb/keydb.conf"
            - "--requirepass"
            - "$({{ .Values.msg.auth.existingSecretKey }})"
          {{- end }}
          {{- with .Values.msg.args }}
          {{- range . }}
            - {{ . | quote }}
          {{- end }}
          {{- end }}
          {{- with .Values.msg.containerSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.name
            {{- if .Values.msg.master.enabled }}
            - name: MASTER
              value: "true"
            {{- end }}
            {{- if .Values.msg.auth }}
            - name: {{ .Values.msg.auth.existingSecretKey }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.msg.auth.existingSecret }}
                  key: {{ .Values.msg.auth.existingSecretKey }}
            {{- end }}
            {{- range $name, $value := .Values.msg.env }}
            - name: {{ $name }}
              value: {{ $value | quote}}
            {{- end }}
            {{- range .Values.msg.envSecret }}
            - name: {{ .name }}
              valueFrom:
                secretKeyRef:
                  name: {{ .secretRef }}
                  key: {{ .secretKey }}
            {{- end }}
          volumeMounts:
            - mountPath: /data
              {{- if .Values.msg.persistentVolume.subPath }}
              subPath: {{ .Values.msg.persistentVolume.subPath }}
              {{- end }}
              name: {{ $pvc }}
            - mountPath: /etc/keydb/keydb.conf
              subPath: keydb.conf
              name: {{ $pvcConf }}
          {{- if .Values.msg.extraVolumeMounts }}
            {{- toYaml .Values.msg.extraVolumeMounts | nindent 12 }}
          {{- end }}
      volumes:
        - name: {{ $pvcConf }}
          configMap:
            name: {{ $pvc }}
        - name: {{ $pvc }}
          {{- if .Values.msg.persistentVolume.enabled }}
          persistentVolumeClaim:
            claimName: {{ if .Values.msg.persistentVolume.existingClaim }}{{ .Values.msg.persistentVolume.existingClaim }}{{- else }}{{ template "qmig.msg.fullname" . }}{{- end }}
          {{- else }}
          emptyDir: {}
          {{- end -}}
      {{- if .Values.msg.extraVolumes }}
        {{- toYaml .Values.msg.extraVolumes | nindent 8 }}
      {{- end }}
